=== PENSE-BETE LOCAPP ===

- revoir la configuration et le bon fonctionnement de la fonction mot de passe oublié
- rediriger vers la page de connexion si les identifiants sont perdus lors du refresh d'une page admin
- verifier qr code et plan dans rendu

=== GENERATION IA PAR SECTION ===

Ajouter un bouton "Générer avec l'IA" dans chaque section du menu de gauche :
- Général (message de bienvenue)
- Adresse (description du quartier)
- Activités (incontournables, sports, restaurants, visites)
- Services (pharmacie, supermarché, boulangerie)
- Parking (informations parking)

Conditions :
- Le bouton n'est ACTIF que si le compte n'est PAS un compte gratuit (plan payant requis)
- Pour les comptes gratuits : bouton grisé avec tooltip "Fonctionnalité réservée aux abonnés"
- Régénère uniquement le contenu de la section concernée (pas toute la propriété)

=== PROCHAINE ETAPE ===

Continuer la gestion de la création d'une nouvelle propriété :

1. CREATION SANS IA (Template)
   - Utiliser "Mazet - Demo" comme base/template
   - L'utilisateur saisit le nom et l'adresse de la propriété
   - Dupliquer toutes les données de Mazet - Demo vers la nouvelle propriété
   - L'utilisateur personnalise ensuite manuellement chaque section

2. CREATION AVEC IA (Automatisée)
   - L'utilisateur saisit uniquement l'adresse de la propriété
   - L'IA génère automatiquement :
     * Nom suggéré pour la propriété
     * Message de bienvenue personnalisé
     * Activités à proximité (via géolocalisation)
     * Services à proximité (pharmacies, supermarchés, etc.)
     * Suggestions de parking
     * Numéros d'urgence locaux
   - Utiliser l'API Google Places ou autre pour enrichir les données
   - Possibilité d'utiliser Claude API pour générer les textes
